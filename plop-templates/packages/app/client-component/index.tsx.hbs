'use client';
import { use{{pascalCase name}} } from "./hooks";
import type { Props } from "./types";
import { Text } from "react-native";

export const {{pascalCase name}} = (props: Props) => {
  use{{pascalCase name}}(props);
  return <Text>{{pascalCase name}}</Text>
};
