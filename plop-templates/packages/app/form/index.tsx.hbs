import { use{{pascalCase name}}Form } from "@/components/{{pascalCase name}}Form/hooks";
import type { Props } from "@/components/{{pascalCase name}}Form/types";
import { Controller } from "react-hook-form";
import { Grid } from "ui/Grid";
import { YStack } from "ui/Stack";
import { Button } from "ui/Button";
import { TextField } from "ui/TextField";
import { VisuallyHidden } from "ui/VisuallyHidden";
import { Form } from "ui/Form";
import { I18nKey } from "shared/libs/i18n";
/**
 * Form Component for create or update {{pascalCase name}}.
 * @param {Props} props {@link Props}
 */
export const {{pascalCase name}}Form = (props: Props) => {
  const {onSubmit, errors, control, t} = use{{pascalCase name}}Form(props);
  return (
  <Form> 
  <YStack gap={"$4"}>
    <Grid.Row>
      <Grid.Column span={4}>
      <Controller
        control={control}
        name="id"
        render={({ field }) => (
          <VisuallyHidden>
            <TextField {...field} label={"id"} error={errors.id?.message && t(errors.id.message as I18nKey)} />
          </VisuallyHidden>
        )}
      />
      </Grid.Column>
      </Grid.Row>
    <Form.Trigger asChild>
    <Button onPress={onSubmit}>
      {t("submit")}
    </Button>
    </Form.Trigger>
  </YStack>
  </Form>)
}
