import { use{{pascalCase name}}Form } from "./hooks";
import type { Props } from "./types";
import { Grid, Stack, Button, TextField, VisuallyHidden, Form } from "@codynog/rn-ui";
import { Controller } from "react-hook-form";
import { I18nKey } from "shared/libs/i18n";
/**
 * Form Component for create or update {{pascalCase name}}.
 * @param {Props} props {@link Props}
 */
export const {{pascalCase name}}Form = (props: Props) => {
  const { onSubmit, errors, control, t } = use{{pascalCase name}}Form(props);
  return (
    <Form onSubmit={onSubmit}>
      <Stack spacing={4} direction="column">
        <Grid>
          <TextField
            label="id"
            errorMessage={errors.id?.message}
          />
        </Grid>
        <Button type="submit">Submit</Button>
      </Stack>
    </Form>
  );
};
