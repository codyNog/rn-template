import { getI18n } from "shared/libs/i18n/server";
import { Typography } from "ui";

// biome-ignore lint:
type Props = {
};

// biome-ignore lint:
export const {{pascalCase name}} = async ({}: Props) => {
    const _t = await getI18n();
return <Typography>{{pascalCase name}}</Typography>
};
